/**
 * Zip arrays into Map
 * 
 * 
 * Given two arrays, create and associative array (aka hash map, an obj / dictionary) containing keys from the first array, and values from the second.
 * 
 * Associative arrays are sometimes called maps because a key (string) maps to a value
 */

const keys1 = ["abc", 3, "yo"];
const vals1 = [42, "wassup", true];
const expected1 = {
    abc: 42,
    3: "wassup",
    yo: true

}

const keys2 = []
const vals2 = []
const expected2 = {}

function zipArraysIntoMap(keys, values) {}

// ===================================================================================================

/**
 * Invert hash
 * 
 * Given an object 
 * return a new object that has the keys and the values swapped.
 * 
 * 
 */

const obj1 = {name: "Zaphod", charm: "high", morals: "dicey"}
const exp1 = {Zaphod: "name", high: "charm", dicey: "morals"}

function invertObj(obj) {
    
}